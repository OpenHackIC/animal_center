development:
  adapter: postgresql
  host: localhost
  database: animal_center_development
  username: vagrant
  password:

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  host: localhost
  database: animal_center_test
  username: vagrant
  password:

# Not actually used in production, but rather just for asset compilation.
production:
  adapter: postgresql
  host: localhost
  database: animal_center_production
  username: vagrant
  password:
